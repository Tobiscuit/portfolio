Vendored from https://github.com/Tobiscuit/lib-ses

Kept as source rather than a dependency so there is no third-party code in the
path between a form submission and a signed AWS call. Import specifiers are
rewritten extensionless for Next's resolver; nothing else is changed.
